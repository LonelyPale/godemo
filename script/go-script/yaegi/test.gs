#!/usr/bin/env yaegi

package main

import (
    "fmt"
    //"github.com/LonelyPale/goutils/crypto"
)

func main() {
	fmt.Println("test")
	fmt.Println("你好，中国！")
}

/*
func testGenSalt() {
    salt, err := crypto.GenerateSalt(SaltLength)
    if err != nil {
        panic(err)
    }
    fmt.Println("salt:", len(salt), len(salt.Hex()), salt.Hex())
}
*/
